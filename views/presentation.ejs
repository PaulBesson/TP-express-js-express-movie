<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>
		<%= film.title %></title>
			<script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
	<%- include ('header.ejs') %>
		<main class="container flex flex-col gap-6 mx-auto m-6 p-6 bg-black/85 rounded-lg shadow-lg">
			<section>
				<h1 class="text-3xl text-white border rounded-lg text-center mt-5 mb-5 bg-white/5 rounded-lg shadow-lg">Films</h1>
					<div class="flex flex-col gap-6 container border rounded-lg p-3 text-center bg-white/5 rounded-lg shadow-lg">
						<h2 class="text-white text-3xl">
							<%= film.title %>
						</h2>
						<img src="../<%= film.image %>" alt="<% film.title %>" class="w-full h-96 object-contain">
						<p class="text-white"><%= film.description %></p>
						<% console.log('favorites:', favorites); %>
						<% if (favorites.some(f => f.id == film.id)) { %>
							<a href="/user/removeFavorites/<%= film.id %>" class="text-white text-center bg-red-600 border inline-block px-4 py-2 rounded-lg hover:bg-red-500">Remove from favorites</a>
							<% } else { %>
							<a href="/user/addFavorites/<%= film.id %>" class="text-white text-center bg-blue-600 border inline-block px-4 py-2 rounded-lg hover:bg-blue-500">Add to favorites</a>
						<% } %>
					</div>
			</section>
		</main>
</body>

</html>
