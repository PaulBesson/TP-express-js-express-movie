<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Document</title>
	<link rel="stylesheet" href="public/style.css">
	<script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
	<%- include ('header.ejs') %>
		<main class="container flex flex-col gap-6 mx-auto m-6 p-6 bg-white/5 rounded-lg shadow-lg">
			<% if (user) { %>
				<h1 class="text-xl text-yellow-600 text-center">
					Hello <%= user.firstname %>
				</h1>
				<% } %>
			<section>
				<h1 class="text-3xl text-yellow-600 border rounded-lg text-center mt-5 mb-5">Films</h1>
				<div class="flex flex-col gap-5">
					<% for (const film of films) { %>
						<div class="border rounded-lg p-3 text-center bg-grey-200">
							<h3>
								<%= film.title %>
							</h3>
								<img src="<%= film.image %>" alt="<% film.title %>" class="w-full h-64 object-contain">
						</div>
						<% } %>
				</div>
			</section>
		</main>
</body>
</html>